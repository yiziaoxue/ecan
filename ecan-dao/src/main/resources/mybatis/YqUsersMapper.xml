<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecan.mapper.YqUsersMapper">

	<insert id="addEntity">
		insert into yq_users(usid,email,password,flag) values(#{usid},#{email},#{password},#{flag})
	</insert>

	<delete id="deleteEntity">
		delete from yq_users where usid = #{usid}
	</delete>

	<update id="updateEntity">
		update yq_users
		<set>
			<if test="usid != null">
				usid = #{usid},
			</if>
			<if test="email != null">
				email = #{email},
			</if>
			<if test="password != null">
				password = #{password},
			</if>
			<if test="flag != null">
				flag = #{flag},
			</if>
		</set>
		where usid = #{usid}
	</update>

	<select id="findEntity" resultType="com.ecan.model.YqUsers">
		select
		usid AS usid,
		email AS email,
		password AS password,
		flag AS flag
		from yq_users
		where 1 = 1
		<if test="usid != null">
			and usid = #{usid}
		</if>
		<if test="email != null">
			and email = #{email}
		</if>
		<if test="password != null">
			and password = #{password}
		</if>
		<if test="flag != null">
			and flag = #{flag}
		</if>
	</select>

	<select id="findEntityList" resultType="com.ecan.model.YqUsers">
		select
		usid AS usid,
		email AS email,
		password AS password,
		flag AS flag
		from yq_users
		where 1 = 1
		<if test="usid != null">
			and usid = #{usid}
		</if>
		<if test="email != null">
			and email = #{email}
		</if>
		<if test="password != null">
			and password = #{password}
		</if>
		<if test="flag != null">
			and flag = #{flag}
		</if>
	</select>


</mapper>